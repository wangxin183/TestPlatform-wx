你是测试用例设计专家。根据以下解析后的需求文档和分析报告，生成全面的测试用例。

解析后的需求文档：
{requirements_json}

分析报告：
{analysis_json}

所有输出内容必须使用中文。

生成覆盖以下方面的测试用例：
- 正常流程场景
- 错误/异常场景
- 边界值测试
- 组件间的集成点
- 分析报告中识别的所有边界情况

输出为 JSON 数组，每个测试用例结构如下：
[
  {
    "title": "简短的描述性标题（中文）",
    "description": "此测试验证的内容（中文）",
    "preconditions": "测试执行前需要的前置条件（中文）",
    "steps": [
      {"step": 1, "action": "描述要执行的操作（中文）", "expected": "描述预期结果（中文）"}
    ],
    "priority": "严重/高/中/低",
    "test_type": "功能/界面/接口/性能/集成",
    "tags": ["冒烟测试", "登录", "支付"],
    "platform_type": "{platform_type}"
  }
]

只输出 JSON 数组，不要输出其他文本。
